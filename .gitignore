node_modules
dist
dist-ssr
*.local
.vite
.DS_Store
*.log

# secrets — never commit
.env
.env.*
!.env.example
SKILL.MD

# local design reference, not part of the app
wireframe.html

# Playwright
test-results/
playwright-report/

# Reference clone of the sibling operator app, dropped in for comparison.
# It carries its own .git, so committing it would leave a submodule pointer
# nobody else can resolve.
perfox-contact-center/
