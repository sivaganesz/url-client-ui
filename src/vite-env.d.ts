/// <reference types="vite/client" />

/**
 * Vite's ambient types: `import.meta.env` and the module declarations for
 * asset imports (`./index.css`, SVGs) that tsc otherwise cannot resolve,
 * since Vite resolves them at build time rather than through node_modules.
 */
